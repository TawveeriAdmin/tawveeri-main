# مسودة ADR-127 — إصلاح هوية المكيّفات: استعادة إلزام `cooling_mode` (تشخيص فقط)

> **Status: Proposed** (تشخيص — لم يُعدّل أي parser). يُنقل إلى `docs/DECISIONS.md` عند الاعتماد.

## Context

تدقيق هوية الـ428 (IDENTITY_VALIDATION + PRICE_INTEGRITY) كشف مكيّفات دُمجت عروضها تحت **مفتاح هوية واحد رغم اختلاف `cooling_mode` الظاهر** (بارد فقط + حار/بارد). المثال القاطع (مقيس اليوم، مفتاح هوية واحد):

- **LG Split AC 18000 BTU** — إكسترا: *"LG, Split AC, 18,000 BTU, **Cold**, Rotary"* (بارد فقط) · المنيع: *"LG Split AC, 18000 BTU, **Cooling & Heating**, Rotary — LK182H0"* (حار/بارد) · نون: *"Split AC 18000 BTU **Cold** Rotary"*. الثلاثة تحت `identity_key` واحد → مقارنة تخلط منتجًا بارد-فقط مع منتج حار/بارد (سعر مختلف جوهريًا).

**العدد المرصود:** فحص آلي (regex) على 34 مكيّفًا قابلًا للمقارنة أشار إلى **6** بعروض cooling متعارضة؛ المراجعة اليدوية تؤكّد **~2–3 دمجًا حقيقيًا** (الباقي إيجابيات كاذبة: كلا العرضين حار/بارد بصياغتين). لكن **آلية الخلل مؤكّدة** ببيانات حيّة، وهي منهجية لا حالة فردية.

**الخرق:** ADR-001 نصّ أن `cooling_mode` حقل هوية حرج وأن **الدمج الخاطئ أخطر من عدم الدمج** («Unknown أفضل من Incorrect»). المحرك يخالف قرارًا معتمدًا.

## Root cause — لماذا سقط الحقل، وأين

**الحقل لم يسقط من المفتاح.** `scripts/tps-plugins/ac/identity.ts`:
- السطر 23: `cooling_mode` ضمن الحقول الحرجة — `null` يُبطل المفتاح (`invalid`).
- السطر 32: `cooling_mode` جزء صريح من المفتاح: `${cb}|${ac_type}|${series}|${capacity_btu}|${tech}|${cooling_mode}`.

**سقطت دقّة استخراجه في الـparser** — `scripts/tps-plugins/ac/parser.ts:73–92`:

1. **أنماط `hot_cold` (73–80) لا تغطّي الترتيب المعكوس:** تلتقط `"heating and cooling"`, `"hot/cold"`, `"heat & cool"` — لكن **لا** تلتقط `"Cooling & Heating"` / `"Cooling and Heating"` (وهي صياغة المنيع الشائعة). فالعنوان الحار/بارد **يفلت** من التصنيف الصحيح.
2. **Patch 2 (89–92) بلا حارس تدفئة:** آخر شرط `combined.includes("cooling") → cool_only` **دون فحص غياب** `heat`/`heating`/`تدفئة`/`حار`. فعنوان *"Cooling & Heating"* — الذي يحوي "cooling" — يُصنّف **`cool_only`** خطأً.
   (الخطوة 4 في السطر 87 تفحص `!hot && !heat`، لكن Patch 2 في السطر 89 **لا** ترثها — فهي المنفذ الذي يمرّ منه الخطأ.)

**النتيجة:** مكيّف حار/بارد يُنسب `cool_only`، فيتطابق مفتاحه مع مكيّف بارد-فقط حقيقي بنفس (brand|type|btu|…) → **دمج خاطئ**. الحقل موجود في المفتاح، لكن قيمته ملوّثة.

## Decision — كيف يُعاد إلزامه (مقترح، لا تنفيذ الآن)

1. **حارس تدفئة عام (الأهم):** أي إشارة تدفئة (`heat`|`heating`|`warm`|`تدفئة`|`تسخين`|`حار`) في العنوان **تمنع `cool_only` نهائيًا**، ويُطبَّق **قبل** Patch 2 وكل خطوات cool_only.
2. **توسيع أنماط `hot_cold`** لتشمل: `"cooling & heating"`, `"cooling and heating"`, `"cool & heat"`, والعربية `"تبريد وتدفئة"` / `"تدفئة وتبريد"`.
3. **عدم التخمين عند الغموض:** إن لم يُحسم cooling_mode بإشارة صريحة (لا بارد-فقط ولا حار/بارد) → **`null`** (مفتاح `invalid`) بدل الافتراض `cool_only`. المكيّف غير المحسوم **لا يُدمج** حتى يُحسم — التزامًا بـ«Unknown أفضل من Incorrect».
4. **إصلاح رجعي (retroactive):** بعد تصحيح الـparser، إعادة تطبيع فئة `air_conditioner` + **فصل الكانونيكال المدموجة خطأً** (لا تكفي إعادة السحب وحدها — الدمج القائم يبقى حتى يُفصل).

## Alternatives (مرفوضة)

- إخراج `cooling_mode` من المفتاح لرفع الـrecall — **مرفوض** (خرق ADR-001 مباشر).
- الاعتماد على فرق السعر لكشف الدمج بعد وقوعه — **مرفوض** (علاج للعرَض لا للسبب؛ ويخفق تحت عتبة 1.4).

## Consequences

- `cooling_mode` يستعيد دقّته؛ المكيّفات بارد-فقط/حار-بارد تنفصل → مقارنات أصدق.
- بعض المكيّفات الغامضة تصبح بلا مفتاح (تخرج من المقارنة حتى تُحسم) → **recall أقل قليلًا مقابل precision أعلى** — وهو الاتجاه الذي يفرضه الدستور.
- يجب أن يسبق أي وصل للمكيّفات (وإلا يظهر "وفّر" يقارن بارد-فقط بحار/بارد).

> تشخيص فقط. لم يُعدّل `parser.ts` ولا أي منطق. التنفيذ يتطلب موافقتك واعتماد ADR-127 + تشغيل الإصلاح الرجعي تحت حارس التدقيق.
