# CONNECT_PLAN — خطة وصل System A بالبحث (اقتراح فقط — لا تنفيذ)

> السياق (ADR-125 + قياسات اليوم): البحث يقرأ فهرس Algolia **`products` (System B)** الذي فيه **0 مقارنة دائمة**؛ المقارنة التي يراها العميل تأتي فقط من تجميع Algolia اللحظي. **System A يحمل 428 مقارنة محلولة (منها 220 أخضر موثوق بعد PRICE_INTEGRITY) لكنها معزولة عن البحث.**
>
> **حقيقة معمارية مهمة تُوجّه القرار:** صفحة المقارنة `/compare/[tps_identity_key]` **تقرأ أصلًا من canonical/System A** (ADR-124)، بينما البحث يقرأ B → **انفصام قائم: البحث على B، المقارنة على A.** أي وصل يجب أن يعالج هذا الانفصام لا أن يعمّقه.

---

## الشرط المسبق المطلق (قبل أي خيار)

**لا وصل قبل تطبيق التصنيف الناتج من IDENTITY_VALIDATION + PRICE_INTEGRITY:**
- 🟢 **220 أخضر** → تُعرض بطاقة مقارنة **مع رقم "وفّر"**.
- 🟡 **131 أصفر** → بطاقة مقارنة **بلا رقم "وفّر"** (لون/كانونيكال مكرر + إكسترا-أرخص-ترويجي).
- 🔴 **77 أحمر** → **تُحجب** (لا تدخل الفهرس المقارَن).

وصلٌ بلا هذا التصنيف = عرض "وفّر" وهمي في اليوم الأول (خرق «Unknown أفضل من Incorrect»).

---

## ★ نتائج التحقق (2026-07-28) — تُعدّل التوصية

### المطلوب 1: مفتاح الربط A↔B — **مانع مطلق يبطل خيار 2**
- **يوجد مفتاح صريح:** `products.canonical_product_id` (uuid) → `canonical_products`. مملوء في **1,461/5,814 (25%)** من products عمومًا. (وجدول `product_links` = **صفّان فقط**، عمليًا فارغ.)
- **لكن على الـ220 الخضراء: `0/220` (صفر) قابلة للوصل بمفتاح صريح.** المفتاح الموجود في B يشير إلى كانونيكال **أخرى** غير التي تحمل المقارنات (الـ428/220). و**0** منها لها ≥2 متجر مربوط في B عبر المفتاح.
- **بقاعدتك "ما لا يُربط بمفتاح صريح لا يدخل الوصل" → عدد الـ220 القابل للوصل بمفتاح موثوق = صفر.**
- **الأثر:** خيار 2 (حقن مقارنات A في فهرس B) كان يتطلب مطابقة B↔A؛ وبما أن المفتاح الصريح لا يغطّي أيًّا من الـ220، فالحقن يعتمد على **مطابقة بالاسم للـ220 كلها** — هشّة وخطيرة (مطابقة اسم خاطئة تربط مقارنة صحيحة بمنتج خاطئ، أسوأ من لا مقارنة). **∴ خيار 2 مرفوض حتى يُبنى مفتاح ربط موثوق.**

### المطلوب 2: الخطر المتبقي داخل الـ220 (غير المقاس سابقًا)
- **إكسترا هي الأرخص في `49/220` (22%)** من الخضراء (بعد استبعاد الـ52 أجهزة). توزيعها: **tv 17 · laptop 13 · mobile 7 · monitor 5 · tablet 4 · smartwatch 3**.
- هذه الـ49 معرّضة لنمط إكسترا الترويجي نفسه (69.99%) إن امتدّ للإلكترونيات — **ولا يمكن التحقق آليًا** (بياناتنا بلا `wasPrice`؛ والتحقق الحيّ عبر Unbxd دائري لأنه مصدر الـparser). **حجم المخاطرة المتبقي = 22% من الأخضر، غير محسوم حتى نسحب سياق الخصم من إكسترا.**

---

## الخيارات

### خيار 1 — تحويل فهرس البحث كليًا إلى `tawveeri_tps_products` (A)
`ALGOLIA_INDEX_NAME=tawveeri_tps_products` + تكييف `algoliaHitToGrouped` لشكل سجل A (`display_name_*`, `stores[]`, `has_comparison`).

| البُعد | التقييم |
|---|---|
| الجهد | متوسط (تكييف mapping) |
| المخاطرة | **عالية على التغطية:** فهرس A = **4,143** سجل مقابل **5,027** في B → البحث يخسر ~884 منتجًا مفردًا موجودًا في B فقط (لم يصل A بعد). تقلّص محسوس لتغطية البحث. |
| التراجع | **فوري** (env واحد يعود لـ`products`) |
| أثر صفحة المقارنة | **إيجابي — يوحّد البحث مع المقارنة** (كلاهما A)؛ يزيل الانفصام |

### خيار 2 — حقن مقارنات A الخضراء داخل فهرس `products` (B) عند البناء ★ الموصى به
تعديل `rebuild-products-index.ts` (الذي يبني `products` من B): يبقى B هو الأساس (تغطية كاملة 5,027)، وتُحقن `stores[]` متعددة المتاجر من A **للعائلات الخضراء فقط** (المطابقة عبر `tps_identity_key` أو أفضل مفتاح متاح)، مع تطبيق التصنيف (أخضر=وفّر، أصفر=بلا وفّر، أحمر=مستبعد).

| البُعد | التقييم |
|---|---|
| الجهد | متوسط (تعديل باني الفهرس + منطق التصنيف + مطابقة B↔A) |
| المخاطرة | متوسطة — **لا كتابة على قاعدة البيانات** (بناء فهرس فقط)؛ الخطر الرئيسي مطابقة B↔A (مفتاح الربط يحتاج تأكيدًا؛ إن غاب فالمطابقة بالاسم هشّة) |
| التراجع | **فوري** (أعد بناء الفهرس من B فقط) |
| أثر صفحة المقارنة | إيجابي — العائلات الخضراء يظهر لها البحث + صفحة مقارنة A متطابقة (الرابط `tps_identity_key` قائم من ADR-124) |
| التغطية | **محفوظة** (5,027 كاملة) — لا خسارة منتجات |

### خيار 3 — كتابة مقارنات A في `product_stores` (دمج على مستوى قاعدة البيانات)
ربط canonical A متعدد المتاجر إلى `product_id` واحد بعروض `product_stores` في B.

| البُعد | التقييم |
|---|---|
| الجهد | عالٍ |
| المخاطرة | **عالية — كتابة على B الحيّة المخدومة؛** خطر تكرار/تلويث الكتالوج؛ الانفصام مع صفحة المقارنة A يبقى |
| التراجع | **صعب** (كتابة بيانات تحتاج تنظيفًا) |
| أثر صفحة المقارنة | لا يعالج الانفصام |

---

## التوصية (مُعدّلة بعد المطلوب 1)

**المطلوب 1 يقلب التوصية السابقة:** خيار 2 كان مرشّحًا، لكن **0/220 ربط صريح** يجعله يعتمد على مطابقة بالاسم — مرفوض بقاعدتك. لذلك:

**التوصية الآن: خيار 1 (تحويل البحث إلى فهرس `tawveeri_tps_products` / A)** — لأنه **الوحيد الذي لا يعتمد على مفتاح ربط B (غير الموجود)**:
- يخدم canonical A مباشرةً، وصفحة المقارنة أصلًا A → **توحيد كامل، يزيل الانفصام** بلا أي مطابقة B↔A.
- يطبّق التصنيف (حجب أحمر/وسم أصفر) على فهرس A قبل التبديل.
- **المقايضة المقبولة مؤقتًا:** خسارة ~884 منتجًا مفردًا (فهرس A = 4,143 مقابل B = 5,027) — منتجات B غير مطبّعة في A؛ تُسدّ لاحقًا بتوسيع A (ADR-003)، أو بفهرس بحث نصّي احتياطي على B للاستعلامات التي تُرجع صفرًا من A.
- تراجع فوري (env واحد يعود لـ`products`).

**البديل الوحيد لخيار 2:** بناء مفتاح ربط A↔B موثوق أولًا (حقن `tps_identity_key` في `products`، أو ملء `canonical_product_id` للـ220 عبر مطابقة موثّقة في `product_links` بـ`confidence`+`is_verified`) — مشروع منفصل يسبق أي حقن.

**خيار 3 مرفوض** (كتابة عالية المخاطرة على B الحيّة).

> **الخلاصة الصريحة:** الوصل **ليس جاهزًا تقنيًا اليوم** للحقن في B (لا مفتاح ربط). المسار الآمن الوحيد الفوري = خيار 1 (التوحيد على A) بمقايضة تغطية، وإلا يُؤجَّل الوصل حتى يُبنى مفتاح الربط.

---

## مسودة ADR-126

> **ADR-126 — وصل مقارنات System A الخضراء إلى البحث عبر حقنها في فهرس `products` (B) · Proposed (2026-07-28)**
>
> **Context:** البحث يقرأ فهرس `products` (B) بلا مقارنة دائمة (0/5,543)؛ System A يحمل 428 مقارنة محلولة، منها **220 أخضر موثوق** بعد تدقيق الهوية (IDENTITY_VALIDATION) والأسعار (PRICE_INTEGRITY). صفحة المقارنة تقرأ أصلًا A → انفصام. العميل لا يرى مقارنات A.
> **Decision:** تعديل `rebuild-products-index.ts` ليحقن `stores[]` متعددة المتاجر من A **للعائلات الخضراء فقط** في فهرس `products`، مع تطبيق التصنيف: أخضر=بطاقة مقارنة بوفّر، أصفر=بلا وفّر، أحمر=مستبعد. لا كتابة على قاعدة البيانات. تُحجب عروض جرير المجدّدة وحالات اللون تُدمج (بندان معتمدان مسبقًا).
> **Alternatives:** تحويل الفهرس كليًا إلى `tawveeri_tps_products` (يخسر ~884 منتجًا مفردًا)؛ الكتابة في `product_stores` (كتابة عالية المخاطرة على B الحيّة). كلاهما مرفوض للمدى القصير.
> **Consequences:** العميل يرى 220 مقارنة موثوقة بتغطية بحث كاملة محفوظة؛ توحيد جزئي مع صفحة المقارنة؛ تراجع فوري بإعادة البناء. التوحيد الكامل على A (ADR-003) يبقى الهدف بعد سدّ فجوة تغطية A.

---

## خطة التراجع

- **خيارا 1 و2:** لا كتابة على قاعدة البيانات → التراجع = إعادة تشغيل `rebuild-products-index.ts` بالوضع الحالي (B فقط) أو إعادة `ALGOLIA_INDEX_NAME=products`. **زمن التراجع: دقائق، بلا فقد بيانات.**
- نقطة تحقّق قبل النشر: بناء الفهرس على فهرس **مؤقت** (`products_staging`) أولًا، والتحقق منه بمعيار `search-success.js` + عيّنة من الـ220 الخضراء، ثم التبديل الذرّي (`ALGOLIA_INDEX_NAME`) — فلا يرى العميل فهرسًا نصف-مبني.
- **حارس ما قبل الوصل:** تشغيل تدقيق الهوية على أي عائلة قبل حقنها (استبعاد أي أحمر جديد يظهر بعد إعادة السحب).

---

## حُرّاس الباني الإلزامية (أضيفت بطلب المؤسس)

**(أ) حارس النضارة — إلزامي، بلا استثناء ولا تعطيل يدوي.**
أي عائلة فيها **عرض واحد أقدم من 7 أيام** (`price_history.observed_at`) تسقط تلقائيًا إلى **أصفر** (تُعرض بلا رقم "وفّر"). المجدول (#20) ما زال معلّقًا، فلا ضمان لنضارة كل الأسعار. (ملاحظة: تدقيق اليوم وجد أحدث سعر لكل عرض نضِرًا 3–6 أيام، لكن الحارس يبقى إلزاميًا لأن المجدول قد يتوقف صامتًا).

**(ب) حارس انتهاء العرض.**
بعد إصلاح parser إكسترا (سحب `endDateDiscount` — بند معتمد لاحقًا)، أي عرض **انتهى `endDateDiscount`** يُستبعد **فورًا** من احتساب "وفّر" ومن العرض. **سعر منتهٍ = لا سعر.** (اليوم: مثال `WindFree` خصمه انتهى فعلًا 2026-07-28، و`vacuum` ينتهي 2026-07-30 — يجب ألا يظهرا بسعرهما الترويجي بعد الانتهاء).

**(ج) بوابة القبول المحدّدة — الرقم الذي يثبت نجاح الوصل.**
`search-success.js` يقيس **الصلة** لا المقارنة؛ الـ54 استعلامًا تعطي اليوم **20 بطاقة متعددة المتاجر**. بعد الحقن على فهرس **staging**، يُعاد قياس هذا الرقم — **ارتفاعه فوق 20 (نحو عدد الـ220 الواصلة فعلًا للاستعلامات) هو الدليل الوحيد على نجاح الوصل**، لا مجرد بناء الفهرس. لا تبديل ذرّي قبل تجاوز هذه البوابة.

**(د) خيار الإطلاق المتحفظ (بطاقة مقارنة بلا رقم "وفّر" إطلاقًا) — التقييم والرأي.**

| البُعد | إطلاق الـ220 **مع** وفّر | إطلاق **بلا** وفّر إطلاقًا |
|---|---|---|
| مخاطر الوفّر الوهمي (إكسترا الترويجي، النضارة، الـ22% غير المقاسة) | قائمة — تتطلب فرزًا دقيقًا وحُرّاسًا | **صفر** — تُزال دفعة واحدة |
| القيمة المعروضة للعميل | مقارنة + رقم توفير جاهز | مقارنة كاملة (منتج+متاجر+أسعار+روابط) — العميل يرى الفرق بنفسه |
| نطاق ما يمكن وصله | الأخضر الموثوق فقط (220) | يتّسع محتمل (الخطر الوحيد كان الرقم) |
| الرسالة التسويقية ("توفيري") | قوية | أضعف (لا رقم توفير) |

**رأيي:** خيار الإطلاق بلا "وفّر" **جيد كطبقة أمان لكنه لا يكفي وحده** — لأن العميل حتى بلا رقم "وفّر" **سيرى منتجًا واحدًا بسعرين متباعدين** في حالات الدمج الأحمر (بارد-فقط مع حار/بارد، مجدّد مع جديد) → ارتباك وفقدان ثقة بلا تفسير. أي: **بلا-وفّر لا يُغني عن حجب الأحمر ودمج اللون.**

**توصيتي — إطلاق على مرحلتين يفصل المخاطرتين:**
- **المرحلة 1:** إطلاق **بلا رقم "وفّر" إطلاقًا** (كل البطاقات مقارنة فقط) **مع حجب الأحمر (77) ودمج اللون (33)**. أدنى مخاطرة، يُظهر 90% من قيمة المقارنة، ويشتري وقتًا لإصلاح تسعير إكسترا. مطبّقًا عليه الحُرّاس (أ)+(ج).
- **المرحلة 2:** تفعيل "وفّر" **للأخضر فقط** بعد **سحب `wasPrice`+`discount`+`endDateDiscount` من إكسترا** (يصبح "وفّر" محسوبًا من خصم حقيقي لا من مقارنة سعر ترويجي بعادي)، وبعد إصلاح دمج المكيّفات (ADR-127).

هذا يفصل "إظهار المقارنة" (منخفض المخاطر، ممكن قريبًا) عن "ادعاء التوفير" (يتطلب إصلاح تسعير إكسترا) — بدل رهن الاثنين معًا.

> كل ما سبق اقتراح مكتوب. لم يُنفَّذ شيء ولم يُعدَّل أي parser أو منطق تصنيف. التنفيذ يتطلب موافقتك.
