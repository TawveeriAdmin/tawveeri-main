[build]
builder = "RAILPACK"
buildCommand = "npm install --legacy-peer-deps && npx tsx scripts/algolia-sync.ts"

[deploy]
startCommand = "echo 'Algolia Sync completed successfully!'"
restartPolicyType = "NEVER"
